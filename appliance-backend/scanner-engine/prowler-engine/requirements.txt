# Prowler engine - runs Prowler rules via direct Python import (no CLI)
# Prowler is installed from prowler-core via: pip install -e ../prowler-core
prowler>=4.0.0
fastapi>=0.104.0
uvicorn>=0.24.0
pytest>=8.0.0
