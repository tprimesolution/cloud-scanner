Guard engine wrapper directory.

This project keeps the upstream CloudSploit integration module in
`src/modules/scanner-engine/cloudsploit` (internal path unchanged by design),
while exposing the rebranded Guard engine at the service/configuration layer.
